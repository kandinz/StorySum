# Thư mục chứa Model ONNX Offline
Đặt các file model ONNX (`.onnx`), config (`.json`), hoặc tokens (`.txt`) vào thư mục này để nạp vào App.
Ví dụ:
- `vits_vietnamese_female.onnx`
- `vits_vietnamese_male.onnx`
